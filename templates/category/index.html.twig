{% extends 'base.html.twig' %}

{% block title %}All categories!{% endblock %}

{% block body %}
    {% for category in categories %}
        <div class="container">
            <h2>{{ category.name }}</h2>
        </div>
    {% endfor %}

{% endblock %}
